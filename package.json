{
  "name": "react-counter-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "esbuild index.tsx --bundle --outfile=dist/bundle.js --servedir=. --watch",
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --minify --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}
